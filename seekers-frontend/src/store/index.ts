/**
 * No global state yet — the landing page is fully static/local-state.
 * When you need shared state (e.g. auth, admissions form progress),
 * this is where a Zustand store / React Context provider would live.
 */
export {};
